# 교육용 POS 앱 설계 문서

> 이 문서는 프로젝트의 단일 기준(source of truth)이다.
> Claude가 작업할 때 항상 이 문서를 먼저 읽고, 결정이 바뀌면 이 문서를 갱신한다.

---

## 1. 프로젝트 개요

- **목적**: 노인분들에게 POS 사용법을 **교육**하기 위한 POS 앱.
  - 실제 매장 운영용이 아니라 교육/연습용이다.
  - 따라서 실제 카드결제(VAN/PG) 연동은 하지 않고, 결제는 시뮬레이션으로 처리한다.
- **구성 요소** (2개):
  1. **포스앱** — Flutter. 교육 대상(노인)이 직접 조작하는 앱.
  2. **관리자페이지** — Flutter Web. 물품 정보 등 데이터를 등록/수정/관리.
- **서버**: Supabase (DB + Auth + API). 관리자페이지에서 등록한 데이터를 포스앱이 읽는다.

## 2. 핵심 설계 원칙 (교육용 앱이므로)

- **노인 친화 UI 최우선**:
  - 큰 글씨, 큰 버튼, 높은 대비.
  - 한 화면에 정보 최소화. 복잡한 제스처(길게 누르기, 스와이프) 지양, 단순 탭 위주.
  - 실수해도 쉽게 되돌릴 수 있어야 함 (명확한 "취소", "뒤로" 버튼).
- **데이터 레이어 교체 가능하게 설계**:
  - 1단계에서 목데이터, 2단계에서 Supabase로 갈아끼운다.
  - 처음부터 Repository 패턴(인터페이스) 뒤에 데이터 소스를 숨겨서,
    `MockRepository` → `SupabaseRepository` 교체만으로 전환되도록 만든다.
  - **UI/비즈니스 로직 코드는 2단계에서 수정하지 않는 것이 목표.**

## 3. 진행 순서 (반드시 이 순서대로)

### 1단계 — 목데이터로 포스앱 완성 (서버 없음)
- [ ] Flutter 프로젝트 셋업 (모노레포 구조, 아래 §5 참조)
- [ ] 목데이터 정의 (상품, 카테고리 등 — §7 데이터 모델과 동일한 스키마로)
- [ ] Repository 인터페이스 + MockRepository 구현
- [ ] POS 핵심 플로우 완성:
  - 상품 목록(카테고리별) → 상품 탭해서 주문 담기 → 주문 목록/합계 확인
  - 수량 변경, 항목 삭제, 전체 취소
  - 결제(시뮬레이션: 현금/카드 선택 → 완료 화면)
- [ ] 가로/세로 레이아웃 둘 다 완성 (태블릿·폰 공통, 방향 기준 — §6)
- [ ] 주문 기록은 메모리에만 (앱 재시작 시 소멸, OK)
- [ ] 이 단계에서 서버/네트워크 코드는 일절 넣지 않는다.

### 2단계 — 서버 + 관리자페이지 구축, 목데이터 제거
- [ ] Supabase 프로젝트 생성, 테이블 스키마 구축 (§7)
- [ ] `shared/` 패키지에 SupabaseRepository 구현
- [ ] 관리자페이지(Flutter Web) 제작:
  - 로그인 (Supabase Auth)
  - 상품 등록/수정/삭제, 카테고리 관리, 가격, 이미지, 판매 여부(노출 on/off)
  - 주문(연습) 기록 조회 + 전체 초기화 버튼
- [ ] 포스앱에서 MockRepository 완전 제거 → SupabaseRepository로 교체
- [ ] 포스앱 주문 완료 시 orders/order_items를 Supabase에 저장
- [ ] 관리자페이지에서 바꾼 내용이 포스앱에 반영되는지 확인

### 3단계 — 검증 및 피드백 핑퐁
- [ ] 전체 플로우가 완벽히 동작하는지 확인 (관리자 등록 → 포스 표시 → 주문 → 결제 → 기록 조회)
- [ ] 사용자(개발자) 피드백 받아서 수정 → 재확인 반복
- [ ] 가이드 모드(단계별 튜토리얼) 도입 여부를 이 단계에서 재검토
- [ ] 이 단계에서 나온 결정 사항은 이 문서에 반영한다

## 4. 확정된 결정 사항

| 항목 | 결정 | 비고 |
|---|---|---|
| 포스앱 | Flutter | |
| 관리자페이지 | **Flutter Web** | 포스앱과 모델·Supabase 코드 공유 |
| 서버 | Supabase | PostgreSQL + Auth + 자동 API |
| 타겟 기기 | **태블릿 우선 + 폰도 지원** | 두 기기 모두 **가로/세로 양방향 지원** |
| 화면 방향 | **가로·세로 모두 지원** | 가로=좌우 분할 레이아웃, 세로=전환형 레이아웃 (§6). 폰 최소 폭 360dp 기준 |
| 교육 가이드 모드 | **1단계 제외** | 자유 연습만. 3단계 피드백 후 도입 재검토 |
| 주문 기록 | **Supabase 저장** (2단계부터) | 관리자페이지에서 조회 + 전체 초기화. 1단계는 메모리만 |
| 프로젝트 위치 | `Desktop/pos-edu/` | 모노레포 (§5) |
| 상태관리 | **Riverpod** | Repository를 provider로 주입 → Mock→Supabase 교체는 provider 오버라이드로 |
| 관리자 계정 | **단일 관리자 계정** | Supabase Auth 이메일 계정 1개. 포스앱은 로그인 없음(anon 읽기 + 주문 쓰기만 허용, RLS로 제한) |

## 5. 프로젝트 구조 (모노레포)

```
Desktop/pos-edu/
├── POS_APP_DESIGN.md      ← 이 문서
├── pos_app/               ← Flutter 포스앱 (태블릿+폰)
├── admin_web/             ← Flutter Web 관리자페이지
└── shared/                ← 공유 Dart 패키지: 데이터 모델, Repository 인터페이스,
                              SupabaseRepository (2단계에서 구현)
```

- MockRepository는 `pos_app/` 안에 두고 2단계에서 삭제한다 (shared에 두지 않음).

## 6. 화면 목록

### 포스앱
1. **메인 판매 화면** — 기기 구분 없이 **화면 방향** 기준으로 레이아웃 결정:
   - 가로: 좌측 카테고리/상품 그리드, 우측 현재 주문 목록 + 합계 + 결제 버튼 (실제 POS 형태)
   - 세로: 상품 그리드 ↔ 주문 목록 전환형 (하단에 합계/결제 바 상시 노출)
   - 회전 시 주문 상태 유지한 채 레이아웃만 전환
2. **결제 화면** — 결제수단 선택(현금/카드), 시뮬레이션 진행
3. **결제 완료 화면** — 큰 글씨로 완료 안내, 처음으로 돌아가기

### 관리자페이지 (Flutter Web)
1. 로그인 (Supabase Auth)
2. 상품 관리 (목록/등록/수정/삭제, 이미지 업로드, 판매 여부 토글)
3. 카테고리 관리
4. 연습(주문) 기록 조회 + 전체 초기화

## 7. 데이터 모델 (초안 — 구현하며 확정)

목데이터도 이 스키마를 그대로 따른다. (2단계 전환을 쉽게 하기 위해)

- **categories** — 카테고리
  - `id`, `name`, `sort_order`
- **products** — 상품
  - `id`, `category_id`, `name`, `price`, `image_url`, `is_active`(판매 여부), `sort_order`
- **orders** — 주문 (연습 기록)
  - `id`, `created_at`, `total_amount`, `payment_method`(cash/card), `status`
- **order_items** — 주문 상세
  - `id`, `order_id`, `product_id`, `product_name`(주문 시점 스냅샷), `price`(스냅샷), `quantity`

## 8. 미결정 사항 (결정되면 §4로 옮기고 여기서 지운다)

- 없음 — 설계 단계의 결정은 모두 완료. 이후 구현 중 생기는 결정은 여기에 추가한다.

## 9. 변경 이력

- 2026-08-16: 최초 작성 (요구사항 정리)
- 2026-08-16: 미결정 5건 확정 — 관리자=Flutter Web, 태블릿 우선+폰 지원, 가이드 모드 1단계 제외, 주문 기록 서버 저장, 폴더 `Desktop/pos-edu/` 모노레포. 문서를 pos-edu/로 이동.
- 2026-08-16: 잔여 3건 확정 — 상태관리=Riverpod, 화면 방향=가로·세로 모두 지원(방향 기준 레이아웃 2벌), 관리자=단일 계정(포스앱은 anon). 미결정 사항 전부 해소.
